---
import Layout from "../layouts/Layout.astro";
---

<Layout
    title="總資產計算機 | 全方位個人淨資產追蹤工具"
    description="視覺化管理您的個人財富，追蹤全球資產淨值 (Net Worth)，整合即時匯率，數據僅儲存於本地端，確保絕對隱私。"
>
    <!-- Structured Data for SEO / AEO -->
    <script type="application/ld+json" is:inline slot="head">
        {
            "@context": "https://schema.org",
            "@graph": [
                {
                    "@type": "SoftwareApplication",
                    "name": "總資產計算機",
                    "applicationCategory": "FinanceApplication",
                    "operatingSystem": "Web",
                    "description": "視覺化管理您的個人財富，追蹤全球資產淨值 (Net Worth)，整合即時匯率，數據僅儲存於本地端，確保絕對隱私。",
                    "offers": {
                        "@type": "Offer",
                        "price": "0",
                        "priceCurrency": "TWD"
                    }
                },
                {
                    "@type": "BreadcrumbList",
                    "itemListElement": [
                        {
                            "@type": "ListItem",
                            "position": 1,
                            "name": "首頁",
                            "item": "https://workfree.vercel.app/"
                        },
                        {
                            "@type": "ListItem",
                            "position": 2,
                            "name": "總資產計算機",
                            "item": "https://workfree.vercel.app/assets-manager"
                        }
                    ]
                }
            ]
        }
    </script>
    <main
        class="container mx-auto px-4 py-8 max-w-7xl relative z-10 min-h-screen"
    >
        <!-- Header -->
        <header
            class="mb-8 md:mb-12 text-center md:text-left flex flex-col md:flex-row items-center justify-between gap-4 animate-fade-in-down"
        >
            <div>
                <h1
                    class="text-4xl md:text-5xl font-extrabold text-slate-900 mb-2 tracking-tight"
                >
                    總資產計算機
                </h1>
                <p class="text-slate-500 text-sm font-medium tracking-wide">
                    您的個人財富儀表板
                </p>
            </div>

            <div
                class="flex items-center gap-2 text-xs font-mono text-slate-600 bg-white px-3 py-1.5 rounded-full border border-slate-200 shadow-sm cursor-pointer hover:bg-slate-50 transition-colors"
                id="exchange-rate-badge"
            >
                <span
                    class="w-2 h-2 rounded-full bg-emerald-500 animate-pulse"
                    id="status-dot"></span>
                <span id="rate-info">正在獲取實時匯率...</span>
            </div>
        </header>

        <div class="grid grid-cols-1 lg:grid-cols-12 gap-8 items-start">
            <!-- Left Column: Asset Lists (lg:col-span-8) -->
            <div class="lg:col-span-8 space-y-8 order-2 lg:order-1">
                <!-- Assets Container -->
                <div id="asset-sections" class="space-y-8 pb-20">
                    <!-- Sections injected by JS -->
                </div>
            </div>

            <!-- Right Column: Sidebar (lg:col-span-4) -->
            <aside
                class="lg:col-span-4 space-y-6 lg:sticky lg:top-8 order-1 lg:order-2"
            >
                <!-- Total Net Worth Card -->
                <div
                    class="p-6 md:p-8 rounded-2xl bg-white border border-slate-100 shadow-lg relative overflow-hidden group hover:shadow-xl transition-all duration-500"
                >
                    <div
                        class="absolute inset-0 bg-gradient-to-br from-blue-50 via-transparent to-transparent opacity-50"
                    >
                    </div>
                    <div class="relative z-10">
                        <h2
                            class="text-slate-500 text-xs tracking-wider uppercase mb-2 font-semibold flex items-center gap-2"
                        >
                            <span class="w-1 h-4 bg-blue-600 rounded-full"
                            ></span>
                            淨資產總額 (TWD)
                        </h2>
                        <div
                            class="text-3xl lg:text-4xl font-bold text-slate-900 tracking-tight tabular-nums break-all"
                            id="total-amount"
                        >
                            0
                        </div>
                        <div
                            id="total-amount-chinese"
                            class="text-sm font-medium text-slate-500 mt-1 tracking-widest"
                        >
                            零元整
                        </div>
                    </div>
                </div>

                <!-- Advice Card -->
                <div
                    id="advice-card"
                    class="p-6 rounded-2xl bg-gradient-to-br from-slate-50 to-white border border-slate-100 shadow-md relative overflow-hidden hidden"
                >
                    <div class="flex items-center gap-2 mb-4">
                        <div class="p-2 bg-blue-100 rounded-lg text-blue-600">
                            <svg
                                xmlns="http://www.w3.org/2000/svg"
                                width="20"
                                height="20"
                                viewBox="0 0 24 24"
                                fill="none"
                                stroke="currentColor"
                                stroke-width="2"
                                stroke-linecap="round"
                                stroke-linejoin="round"
                                ><circle cx="12" cy="12" r="10"></circle><line
                                    x1="12"
                                    y1="16"
                                    x2="12"
                                    y2="12"></line><line
                                    x1="12"
                                    y1="8"
                                    x2="12.01"
                                    y2="8"></line></svg
                            >
                        </div>
                        <h3 class="text-lg font-bold text-slate-800">
                            資產建議
                        </h3>
                    </div>
                    <div id="advice-content" class="space-y-3">
                        <!-- Advice items injected by JS -->
                    </div>
                </div>

                <!-- Chart Card -->
                <div
                    class="p-6 rounded-2xl bg-white border border-slate-100 shadow-md relative overflow-hidden"
                >
                    <h3
                        class="text-slate-500 text-xs tracking-wider uppercase mb-4 font-semibold flex items-center gap-2"
                    >
                        <span class="w-1 h-4 bg-purple-500 rounded-full"></span>
                        資產分佈
                    </h3>
                    <div
                        class="relative h-64 w-full flex items-center justify-center"
                    >
                        <canvas id="assetChart"></canvas>
                    </div>
                    <!-- Legend Container / Details if needed -->
                    <div
                        id="chart-legend"
                        class="mt-4 grid grid-cols-2 gap-2 text-xs text-gray-400"
                    >
                        <!-- Dynamic details -->
                    </div>
                </div>
            </aside>
        </div>

        <!-- Educational Content Section for AEO -->
        <section
            class="mt-24 max-w-4xl mx-auto space-y-16 border-t border-slate-100 pt-16 pb-24"
        >
            <div class="space-y-8">
                <h2
                    class="text-3xl font-extrabold text-slate-900 tracking-tight text-center"
                >
                    為什麼您需要追蹤「淨資產」？
                </h2>

                <div class="grid grid-cols-1 md:grid-cols-2 gap-8">
                    <div class="space-y-4">
                        <h3
                            class="text-xl font-bold text-slate-900 flex items-center gap-2"
                        >
                            <span class="w-1.5 h-6 bg-blue-600 rounded-full"
                            ></span>
                            淨資產：最真實的財務健康指標
                        </h3>
                        <p class="text-slate-600 leading-relaxed text-sm">
                            淨資產（Net
                            Worth）等於您的「總資產」減去「總負債」。相較於月收入，淨資產更能反映您積累財富的真實進度。透過定期追蹤，您可以確認自己是否正在通往財務自由的正確道路上。
                        </p>
                    </div>
                    <div class="space-y-4">
                        <h3
                            class="text-xl font-bold text-slate-900 flex items-center gap-2"
                        >
                            <span class="w-1.5 h-6 bg-blue-600 rounded-full"
                            ></span>
                            告別繁瑣記帳，專注「大帳單」
                        </h3>
                        <p class="text-slate-600 leading-relaxed text-sm">
                            如果您覺得每天記錄幾十塊的消費太痛苦，資產管理是更好的替代方案。每月只需花
                            5
                            分鐘更新一次各帳戶與資產的大數值，就能掌握整體的財務動向，專注於長期的資產配置。
                        </p>
                    </div>
                </div>
            </div>

            <div class="bg-slate-900 rounded-[2.5rem] p-8 md:p-12 text-white">
                <h3
                    class="text-2xl font-extrabold text-blue-400 mb-8 text-center"
                >
                    資產管理 FAQ
                </h3>
                <div class="space-y-6">
                    <div
                        class="bg-white/5 rounded-2xl p-6 border border-white/10"
                    >
                        <h4 class="text-lg font-bold mb-2 text-white">
                            可以使用不同幣別嗎？
                        </h4>
                        <p class="text-slate-400 text-sm">
                            可以。系統支援主流外幣（如 USD, JPY, EUR
                            等）。您只需在輸入資產時選擇幣別，計算機會自動抓取當前即時匯率，統一轉換成您的主貨幣進行統計。
                        </p>
                    </div>
                    <div
                        class="bg-white/5 rounded-2xl p-6 border border-white/10"
                    >
                        <h4 class="text-lg font-bold mb-2 text-white">
                            資料會被第三方看到嗎？
                        </h4>
                        <p class="text-slate-400 text-sm">
                            絕對不會。您的所有資產分佈、金額與備註均儲存在您裝置的
                            IndexedDB
                            資料庫中。本站沒有中央資料庫，因此我們無法獲取、存取或轉售您的任何財務數據。
                        </p>
                    </div>
                </div>
            </div>
        </section>
    </main>

    <!-- Template for Section -->
    <template id="section-template">
        <section
            class="asset-section relative animate-fade-in bg-white rounded-2xl border border-slate-200 shadow-sm p-1"
        >
            <div class="flex items-center justify-between p-4 md:p-6 pb-2">
                <div class="flex items-center gap-3">
                    <div
                        class="section-icon w-10 h-10 rounded-xl bg-slate-50 flex items-center justify-center text-2xl border border-slate-100"
                    >
                    </div>
                    <div>
                        <h3
                            class="section-title text-lg font-bold text-slate-900 tracking-wide"
                        >
                        </h3>
                        <p
                            class="section-subtitle text-[10px] text-slate-500 uppercase tracking-widest"
                        >
                        </p>
                    </div>
                </div>
                <div class="text-right">
                    <div
                        class="section-total text-xl md:text-2xl font-bold font-mono text-blue-600 tabular-nums"
                    >
                        $0
                    </div>
                </div>
            </div>

            <div class="section-list space-y-2 p-2 min-h-[50px]">
                <!-- Rows go here -->
            </div>

            <div class="p-2 pt-0">
                <button
                    class="add-btn w-full py-3 rounded-xl border border-dashed border-slate-300 text-slate-600 hover:border-blue-400 hover:text-blue-700 hover:bg-white hover:shadow-sm transition-all duration-200 flex items-center justify-center gap-2 text-sm font-medium group bg-white/60"
                >
                    <span
                        class="bg-slate-200 p-0.5 rounded group-hover:bg-blue-100 transition-colors text-slate-600 group-hover:text-blue-600"
                    >
                        <svg
                            xmlns="http://www.w3.org/2000/svg"
                            width="16"
                            height="16"
                            viewBox="0 0 24 24"
                            fill="none"
                            stroke="currentColor"
                            stroke-width="2"
                            stroke-linecap="round"
                            stroke-linejoin="round"
                            ><line x1="12" y1="5" x2="12" y2="19"></line><line
                                x1="5"
                                y1="12"
                                x2="19"
                                y2="12"></line></svg
                        >
                    </span>
                    新增項目
                </button>
            </div>
        </section>
    </template>

    <!-- Template for Asset Row -->
    <template id="asset-row-template">
        <div
            class="asset-row group flex flex-col md:flex-row gap-4 p-4 rounded-xl bg-white border border-slate-200 hover:border-blue-300 hover:shadow-md transition-all duration-200 relative shadow-sm"
        >
            <!-- Delete Button -->
            <button
                class="delete-btn absolute top-2 right-2 p-1.5 text-gray-600 hover:text-red-400 hover:bg-red-500/10 rounded-lg transition-colors opacity-0 group-hover:opacity-100 focus:opacity-100 z-10"
                title="刪除"
            >
                <svg
                    xmlns="http://www.w3.org/2000/svg"
                    width="16"
                    height="16"
                    viewBox="0 0 24 24"
                    fill="none"
                    stroke="currentColor"
                    stroke-width="2"
                    stroke-linecap="round"
                    stroke-linejoin="round"
                    ><path d="M3 6h18"></path><path
                        d="M19 6v14c0 1-1 2-2 2H7c-1 0-2-1-2-2V6"></path><path
                        d="M8 6V4c0-1 1-2 2-2h4c1 0 2 1 2 2v2"></path></svg
                >
            </button>

            <!-- Name & Currency (Type is hidden/fixed per section) -->
            <div
                class="flex flex-col sm:flex-row gap-3 items-start sm:items-center flex-grow"
            >
                <div class="w-full sm:w-autoflex-grow min-w-[150px]">
                    <label
                        class="text-[10px] text-slate-600 font-bold mb-1 block uppercase tracking-wider pl-1"
                        >項目名稱</label
                    >
                    <input
                        type="text"
                        class="asset-name w-full bg-slate-50 border border-slate-200 text-slate-900 text-sm rounded-lg px-3 py-2 outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-100 transition-all placeholder-slate-400 hover:bg-white"
                        placeholder="輸入名稱..."
                    />
                </div>

                <div class="w-full sm:w-28 flex-shrink-0">
                    <label
                        class="text-[10px] text-slate-600 font-bold mb-1 block uppercase tracking-wider pl-1"
                        >幣別</label
                    >
                    <div class="relative">
                        <select
                            class="asset-currency w-full bg-slate-50 border border-slate-200 text-slate-900 text-sm rounded-lg px-3 py-2 outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-100 transition-all appearance-none cursor-pointer hover:bg-white font-mono"
                        >
                            <option value="TWD">🇹🇼 TWD</option>
                            <option value="USD">🇺🇸 USD</option>
                            <option value="JPY">🇯🇵 JPY</option>
                            <option value="EUR">🇪🇺 EUR</option>
                            <option value="CNY">🇨🇳 CNY</option>
                            <option value="HKD">🇭🇰 HKD</option>
                        </select>
                        <div
                            class="absolute right-2 top-1/2 -translate-y-1/2 pointer-events-none text-gray-600 scale-75"
                        >
                            <svg
                                xmlns="http://www.w3.org/2000/svg"
                                width="16"
                                height="16"
                                viewBox="0 0 24 24"
                                fill="none"
                                stroke="currentColor"
                                stroke-width="2"
                                stroke-linecap="round"
                                stroke-linejoin="round"
                                ><polyline points="6 9 12 15 18 9"
                                ></polyline></svg
                            >
                        </div>
                    </div>
                </div>
            </div>

            <!-- Dynamic Inputs -->
            <div
                class="dynamic-inputs grid grid-cols-2 gap-3 w-full lg:w-[45%] items-end"
            >
                <!-- Inputs injected by JS -->
            </div>

            <!-- Row Total -->
            <div
                class="flex-shrink-0 flex flex-col justify-end items-end min-w-[100px] border-l border-slate-100 pl-4 hidden md:flex"
            >
                <span
                    class="text-[10px] text-slate-400 uppercase tracking-wider mb-0.5"
                    >估算 (TWD)</span
                >
                <span
                    class="asset-row-total text-base font-bold font-mono text-slate-700"
                    >$0</span
                >
            </div>
        </div>
    </template>
</Layout>

<script>
    import Chart from "chart.js/auto";

    interface Details {
        amount?: number;
        shares?: number;
        pricePerShare?: number;
        coins?: number;
        pricePerCoin?: number;
        area?: number;
        pricePerPing?: number;
        totalValue?: number;
        balance?: number;
    }

    interface Asset {
        id: string;
        type: string;
        name: string;
        currency: string;
        details: Details;
    }

    // Config for Sections
    const ASSET_TYPES = [
        {
            id: "cash",
            icon: "💰",
            title: "現金 & 存款",
            sub: "銀行帳戶",
            color: "#10B981",
            placeholder: "玉山銀行定存 / 數位帳戶",
        }, // Emerald
        {
            id: "stock",
            icon: "📈",
            title: "股票 & 證券",
            sub: "台股 / 美股 / ETF",
            color: "#3B82F6",
            placeholder: "0050 / 台積電 / VTI",
        }, // Blue
        {
            id: "real_estate",
            icon: "🏠",
            title: "房地產",
            sub: "自住 / 投資",
            color: "#8B5CF6",
            placeholder: "台北市大安區自住房 / 竹北收租套房",
        }, // Violet
        {
            id: "car",
            icon: "🚗",
            title: "汽機車",
            sub: "交通工具估值",
            color: "#EC4899",
            placeholder: "Tesla Model 3 / Gogoro",
        }, // Pink
        {
            id: "insurance",
            icon: "🛡️",
            title: "保險",
            sub: "保單現值",
            color: "#EF4444",
            placeholder: "XX 人壽儲蓄險 / 投資型保單",
        }, // Red
        {
            id: "other",
            icon: "💎",
            title: "其他資產",
            sub: "其他有價物",
            color: "#6366F1",
            placeholder: "勞力士手錶 / 實體黃金",
        }, // Indigo
        {
            id: "liability",
            icon: "💳",
            title: "負債",
            sub: "貸款 / 信用卡",
            color: "#64748B",
            placeholder: "車貸 / 房貸 / 信用卡",
        }, // Slate
    ];

    let assets: Asset[] = [];
    let rates: Record<string, number> = {
        TWD: 1,
        USD: 32.5,
        JPY: 0.21,
        EUR: 35.2,
        CNY: 4.5,
        HKD: 4.2,
    };
    const API_URL = "https://open.er-api.com/v6/latest/USD";
    let chartInstance: Chart | null = null;

    // Elements
    const sectionsContainer = document.getElementById("asset-sections");
    const totalAmountEl = document.getElementById("total-amount");
    const statusDot = document.getElementById("status-dot");
    const rateInfo = document.getElementById("rate-info");
    const sectionTemplate = document.getElementById(
        "section-template",
    ) as HTMLTemplateElement;
    const rowTemplate = document.getElementById(
        "asset-row-template",
    ) as HTMLTemplateElement;

    // --- Init ---
    document.addEventListener("DOMContentLoaded", () => {
        initSections();
        initChart();
        loadAssets(); // Triggers render & chart update
        fetchRates();
        setInterval(fetchRates, 300000);
    });

    function initChart() {
        const ctx = document.getElementById("assetChart") as HTMLCanvasElement;
        if (!ctx) return;

        Chart.defaults.color = "#64748b"; // slate-500
        Chart.defaults.font.family = "'Inter', sans-serif";

        chartInstance = new Chart(ctx, {
            type: "doughnut",
            data: {
                labels: ASSET_TYPES.map((t) => t.title),
                datasets: [
                    {
                        data: ASSET_TYPES.map(() => 0),
                        backgroundColor: ASSET_TYPES.map((t) => t.color),
                        borderWidth: 0,
                        hoverOffset: 4,
                    },
                ],
            },
            options: {
                responsive: true,
                maintainAspectRatio: false,
                cutout: "70%",
                animation: false,
                plugins: {
                    legend: {
                        display: false,
                    },
                    tooltip: {
                        backgroundColor: "rgba(0, 0, 0, 0.8)",
                        padding: 12,
                        cornerRadius: 8,
                        titleFont: { size: 14, weight: "bold" },
                        bodyFont: { size: 13 },
                        callbacks: {
                            label: function (context) {
                                let label = context.label || "";
                                if (label) {
                                    label += ": ";
                                }
                                if (context.parsed !== null) {
                                    label += new Intl.NumberFormat("zh-TW", {
                                        style: "currency",
                                        currency: "TWD",
                                        maximumFractionDigits: 0,
                                    }).format(context.parsed);
                                }
                                return label;
                            },
                        },
                    },
                },
            },
        });
    }

    function updateChart(typeTotals: Record<string, number>) {
        if (!chartInstance) return;

        const assetOnlyData = ASSET_TYPES.map((t) => {
            if (t.id === "liability") return 0;
            return Math.max(0, typeTotals[t.id] || 0);
        });

        chartInstance.data.datasets[0].data = assetOnlyData;
        chartInstance.update();
    }

    function initSections() {
        if (!sectionsContainer || !sectionTemplate) return;
        sectionsContainer.innerHTML = "";

        ASSET_TYPES.forEach((type) => {
            const clone = sectionTemplate.content.cloneNode(
                true,
            ) as DocumentFragment;
            const section = clone.querySelector("section");
            if (!section) return;

            section.id = `section-${type.id}`;
            section.querySelector(".section-icon")!.textContent = type.icon;
            section.querySelector(".section-title")!.textContent = type.title;
            section.querySelector(".section-subtitle")!.textContent = type.sub;

            const btn = section.querySelector(".add-btn") as HTMLButtonElement;
            btn.onclick = () => addAsset(type.id);
            btn.innerHTML = `<span class="bg-slate-200 p-0.5 rounded group-hover:bg-blue-100 transition-colors text-slate-600 group-hover:text-blue-600"><svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><line x1="12" y1="5" x2="12" y2="19"></line><line x1="5" y1="12" x2="19" y2="12"></line></svg></span> 新增${type.title}`;

            sectionsContainer.appendChild(section);
        });
    }

    function calculateAssetRowValue(asset: Asset): number {
        const d = asset.details;
        switch (asset.type) {
            case "stock":
                return (d.shares || 0) * (d.pricePerShare || 0);
            case "real_estate":
                if (d.area && d.pricePerPing) {
                    return d.area * d.pricePerPing;
                }
                return d.totalValue || 0;
            case "liability":
                return -Math.abs(d.amount || 0);
            default:
                return d.amount || 0;
        }
    }

    function calculateTotal() {
        let grandTotal = 0;
        const typeTotals: Record<string, number> = {};

        assets.forEach((asset) => {
            const rawVal = calculateAssetRowValue(asset);
            const rate = rates[asset.currency] || 1;
            const valInTwd = rawVal * rate;

            grandTotal += valInTwd;
            typeTotals[asset.type] = (typeTotals[asset.type] || 0) + valInTwd;

            const rowEl = document.querySelector(
                `.asset-row[data-id="${asset.id}"]`,
            );
            if (rowEl) {
                const totalEl = rowEl.querySelector(".asset-row-total");
                if (totalEl)
                    totalEl.textContent = valInTwd.toLocaleString(undefined, {
                        minimumFractionDigits: 0,
                        maximumFractionDigits: 0,
                    });
            }
        });

        ASSET_TYPES.forEach((t) => {
            const sec = document.getElementById(`section-${t.id}`);
            if (sec) {
                const totalEl = sec.querySelector(".section-total");
                const val = typeTotals[t.id] || 0;
                if (totalEl) {
                    totalEl.textContent = val.toLocaleString(undefined, {
                        style: "currency",
                        currency: "TWD",
                        minimumFractionDigits: 0,
                    });
                    if (val < 0) totalEl.classList.add("text-red-400");
                    else totalEl.classList.remove("text-red-400");
                }
            }
        });

        if (totalAmountEl) {
            const fmt = new Intl.NumberFormat("zh-TW", {
                style: "currency",
                currency: "TWD",
                minimumFractionDigits: 0,
                maximumFractionDigits: 0,
            }).format(Math.round(grandTotal));
            totalAmountEl.textContent = fmt.replace("TWD", "$");
        }

        updateChart(typeTotals);
        generateAdvice(typeTotals, grandTotal);
    }

    function generateAdvice(
        typeTotals: Record<string, number>,
        grandTotal: number,
    ) {
        const adviceCard = document.getElementById("advice-card");
        const adviceContent = document.getElementById("advice-content");
        if (!adviceCard || !adviceContent) return;

        const tips: {
            text: string;
            type: "info" | "warning" | "success";
            icon: string;
        }[] = [];

        // Calculate key metrics
        const totalAssets = Object.entries(typeTotals).reduce(
            (acc, [type, val]) => (type !== "liability" ? acc + val : acc),
            0,
        );
        const liabilities = Math.abs(typeTotals["liability"] || 0);
        const netWorth = grandTotal;
        const cash = typeTotals["cash"] || 0;
        const stocks = typeTotals["stock"] || 0;

        // If no assets at all
        if (totalAssets === 0 && liabilities === 0) {
            adviceCard.classList.add("hidden");
            return;
        }

        adviceCard.classList.remove("hidden");

        // 1. High Cash Level (> 60% of total assets)
        if (totalAssets > 0 && cash / totalAssets > 0.6) {
            tips.push({
                text: "現金水位較高 (超過 60%)，在通膨環境下購買力可能受損。建議可適度配置低風險投資工具（如長天期美債 ETF 或高利定存）。",
                type: "info",
                icon: "💰",
            });
        }

        // 2. Low Liquidity (< 10% of total assets)
        if (totalAssets > 0 && cash / totalAssets < 0.1) {
            tips.push({
                text: "流動性資產佔比偏低（低於 10%）。建議預留 3-6 個月的緊急預備金，以應對突發狀況。",
                type: "warning",
                icon: "🚨",
            });
        }

        // 3. High Debt Ratio (Liabilities / Total Assets > 40%)
        if (totalAssets > 0 && liabilities / totalAssets > 0.4) {
            tips.push({
                text: "負債比率較高 (超過 40%)。建議優先償還高利率債務（如信用卡循環利息），以減輕財務負擔。",
                type: "warning",
                icon: "💳",
            });
        }

        // 4. Investment Gaps
        if (netWorth > 500000 && stocks === 0) {
            tips.push({
                text: "淨資產已達一定規模但尚未配置股權類資產。建議可從追蹤大盤的指數型 ETF (如 0050 或 VTI) 開始，參與長期經濟增長。",
                type: "info",
                icon: "📈",
            });
        }

        // 5. Heavy Concentration (Any category > 80% except real estate)
        for (const [type, val] of Object.entries(typeTotals)) {
            if (
                type !== "real_estate" &&
                type !== "liability" &&
                totalAssets > 0 &&
                val / totalAssets > 0.8
            ) {
                const typeName =
                    ASSET_TYPES.find((t) => t.id === type)?.title || type;
                tips.push({
                    text: `資產過度集中於「${typeName}」。建議適度分散投資組合，降低單一市場或資產類別帶來的波動風險。`,
                    type: "info",
                    icon: "⚖️",
                });
            }
        }

        // 6. Good Status / Encouragement
        if (tips.length === 0) {
            tips.push({
                text: "資產配置目前看起來相當穩健！繼續保持定期盤點與紀錄的習慣。",
                type: "success",
                icon: "✨",
            });
        }

        // Render Tips
        adviceContent.innerHTML = tips
            .map((tip) => {
                const colors = {
                    info: "bg-blue-50 text-blue-700 border-blue-100",
                    warning: "bg-amber-50 text-amber-700 border-amber-100",
                    success:
                        "bg-emerald-50 text-emerald-700 border-emerald-100",
                };
                return `
                <div class="flex gap-3 p-3 rounded-xl border ${colors[tip.type]} animate-fade-in">
                    <span class="text-lg flex-shrink-0">${tip.icon}</span>
                    <p class="text-xs leading-relaxed font-medium">${tip.text}</p>
                </div>
            `;
            })
            .join("");
    }

    function renderDynamicInputs(asset: Asset, container: HTMLElement) {
        container.innerHTML = "";

        const createInput = (
            label: string,
            field: keyof Details,
            placeholder = "0",
            step = "any",
        ) => {
            const wrapper = document.createElement("div");

            const labelEl = document.createElement("label");
            labelEl.className =
                "text-[10px] text-slate-600 font-bold mb-1 block uppercase tracking-wider pl-1";
            labelEl.textContent = label;

            const input = document.createElement("input");
            input.type = "number";
            input.inputMode = "decimal";
            input.className =
                "w-full bg-slate-50 border border-slate-200 text-slate-900 text-sm rounded-lg px-3 py-2 outline-none focus:border-blue-400 focus:ring-2 focus:ring-blue-100 transition-all font-mono hover:bg-white";
            input.placeholder = placeholder;
            input.step = step;
            // @ts-ignore
            input.value = asset.details[field]?.toString() || "";

            input.addEventListener("input", (e) => {
                const val = parseFloat((e.target as HTMLInputElement).value);
                // @ts-ignore
                asset.details[field] = isNaN(val) ? 0 : val;
                calculateTotal();
                saveAssets();
            });

            wrapper.appendChild(labelEl);
            wrapper.appendChild(input);
            return wrapper;
        };

        switch (asset.type) {
            case "stock":
                container.appendChild(createInput("股數", "shares"));
                container.appendChild(createInput("現價", "pricePerShare"));
                break;
            case "real_estate":
                container.appendChild(createInput("坪數", "area"));
                container.appendChild(createInput("單價/坪", "pricePerPing"));
                break;
            case "liability":
                container.appendChild(createInput("剩餘金額", "amount"));
                break;
            default:
                container.appendChild(createInput("金額", "amount", "0"));
                break;
        }
    }

    function renderRow(asset: Asset) {
        if (!rowTemplate) return;

        const sectionList = document.querySelector(
            `#section-${asset.type} .section-list`,
        );
        if (!sectionList) return;

        const clone = rowTemplate.content.cloneNode(true) as DocumentFragment;
        const row = clone.querySelector(".asset-row") as HTMLElement;
        row.dataset.id = asset.id;

        const nameInput = row.querySelector(".asset-name") as HTMLInputElement;
        const currencySelect = row.querySelector(
            ".asset-currency",
        ) as HTMLSelectElement;
        const deleteBtn = row.querySelector(".delete-btn") as HTMLButtonElement;
        const dynamicInputsContainer = row.querySelector(
            ".dynamic-inputs",
        ) as HTMLElement;

        nameInput.value = asset.name;
        const typeConfig = ASSET_TYPES.find((t) => t.id === asset.type);
        nameInput.placeholder = typeConfig?.placeholder || "項目名稱";
        currencySelect.value = asset.currency;

        renderDynamicInputs(asset, dynamicInputsContainer);

        nameInput.addEventListener("input", (e) => {
            asset.name = (e.target as HTMLInputElement).value;
            saveAssets();
        });

        currencySelect.addEventListener("change", (e) => {
            asset.currency = (e.target as HTMLSelectElement).value;
            calculateTotal();
            saveAssets();
        });

        deleteBtn.addEventListener("click", () => {
            deleteAsset(asset.id);
            row.remove();
            calculateTotal();
        });

        sectionList.appendChild(row);
    }

    function addAsset(type: string) {
        const newAsset: Asset = {
            id: Date.now().toString(),
            type: type,
            name: "",
            currency: "TWD",
            details: { amount: 0 },
        };
        assets.push(newAsset);
        renderRow(newAsset);
        saveAssets();
        calculateTotal();
    }

    function deleteAsset(id: string) {
        assets = assets.filter((a) => a.id !== id);
        saveAssets();
    }

    function saveAssets() {
        localStorage.setItem("my_assets_v3", JSON.stringify(assets));
    }

    function loadAssets() {
        document
            .querySelectorAll(".section-list")
            .forEach((el) => (el.innerHTML = ""));

        const saved = localStorage.getItem("my_assets_v3");
        if (saved) {
            try {
                assets = JSON.parse(saved);
                assets.forEach(renderRow);
            } catch (e) {}
        } else {
            const v2 = localStorage.getItem("my_assets_v2");
            if (v2) {
                try {
                    assets = JSON.parse(v2);
                    assets.forEach(renderRow);
                    saveAssets();
                } catch (e) {}
            }
        }
        calculateTotal();
    }

    async function fetchRates() {
        try {
            const res = await fetch(API_URL);
            const d = await res.json();
            if (d && d.rates) {
                const usdToTwd = d.rates["TWD"];
                const newRates: any = {};
                for (const [curr, rateAgainstUsd] of Object.entries(d.rates)) {
                    newRates[curr] = usdToTwd / (rateAgainstUsd as number);
                }
                rates = newRates;
                updateStatus(true);
                calculateTotal();
            }
        } catch (e) {
            updateStatus(false);
        }
    }

    function updateStatus(success: boolean) {
        if (!statusDot || !rateInfo) return;
        if (success) {
            statusDot.className =
                "w-2 h-2 rounded-full bg-emerald-500 shadow-[0_0_8px_rgba(16,185,129,0.6)]";
            rateInfo.textContent = "匯率已更新";
        } else {
            statusDot.className = "w-2 h-2 rounded-full bg-red-500";
            rateInfo.textContent = "使用離線匯率";
        }
    }
</script>

<style>
    @keyframes fadeIn {
        from {
            opacity: 0;
            transform: translateY(10px);
        }
        to {
            opacity: 1;
            transform: translateY(0);
        }
    }
    .animate-fade-in {
        animation: fadeIn 0.4s ease-out forwards;
    }
    .animate-fade-in-down {
        animation: fadeIn 0.6s ease-out forwards;
        transform-origin: top;
    }
</style>
